name: basic-ml-app
services:
  backend:
    container_name: intent-clf-container
    build:
      context: .
      dockerfile: app/app.Dockerfile
    ports:
      - "8000:8000"
    env_file: .env
    restart: unless-stopped